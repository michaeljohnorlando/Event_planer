<h1> This will be the home page </h1>

<% @current_session = Session.find_by session_id: cookies[:session_id] %>
<% if @current_session %>
  <%  puts "your cookes session id matches one in the databace" %>
  <%  @user = User.find_by id: @current_session.user_id %>
  <p><%= @user.name %>: is currently logged in</p>
<% else %>
  <p><%=  "No User is loged in!" %></p>
  <p>current: <%= @current_session %></p>
  <p>session: <%=cookies[:session_id] %></p>
<% end %>
